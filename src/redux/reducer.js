export const initState = {
    isLogin: false,
    account: null,
    chainId: 1
}

export const reducer = (state, action) => {
    switch (action.type) {
    

        default:
            return state
    }
}