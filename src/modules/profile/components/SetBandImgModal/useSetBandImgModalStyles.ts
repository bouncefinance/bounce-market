import { makeStyles, Theme } from '@material-ui/core';

export const useSetBandImgModalStyles = makeStyles<Theme>(theme => ({
  root: {
    maxWidth: 700,
  },

  fileBox: {
    height: 200,
  },
}));
