import { makeStyles, Theme } from '@material-ui/core';

export const useWrongWalletModalStyles = makeStyles<Theme>(theme => ({
  root: {},
  logo: {
    fontSize: 68,
    marginTop: theme.spacing(3),
  },
}));
